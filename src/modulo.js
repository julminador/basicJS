const hello = () => {
  console.log('Hello!');
}

export default hello;

// para exportar es necesario agregar en package.json la propiedad
// "type": "module" o guardar el fichero del modulo con extension .mjs